<?xml version='1.0' encoding='UTF-8'?>
<rss xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" version="2.0"><channel><title>Lin's Blog</title><link>https://linarisx.github.io/lynn.github.io</link><description>Welcome to my blog, where I share my learning and practical experiences in game development</description><copyright>Lin's Blog</copyright><docs>http://www.rssboard.org/rss-specification</docs><generator>python-feedgen</generator><image><url>https://avatars.githubusercontent.com/u/206855738?v=4&amp;size=64</url><title>avatar</title><link>https://linarisx.github.io/lynn.github.io</link></image><lastBuildDate>Sat, 04 Jul 2026 19:22:14 +0000</lastBuildDate><managingEditor>Lin's Blog</managingEditor><ttl>60</ttl><webMaster>Lin's Blog</webMaster><item><title>unity跨平台基本原理</title><link>https://linarisx.github.io/lynn.github.io/post/unity-kua-ping-tai-ji-ben-yuan-li.html</link><description>&lt;img width='1615' height='809' alt='Image' src='https://github.com/user-attachments/assets/1607844a-dffd-4dfe-8739-0004eea4d0a9' /&gt;。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/unity-kua-ping-tai-ji-ben-yuan-li.html</guid><pubDate>Sat, 04 Jul 2026 19:21:47 +0000</pubDate></item><item><title>关于.Net</title><link>https://linarisx.github.io/lynn.github.io/post/guan-yu-.Net.html</link><description>.Net是微软提供的技术平台的代号

**框架体系 ：**.Net Framework、 .Net core、 mono等
**开发语言：**C# VB F#等等

微软做.Net平台的目的：跨语言、跨平台

**如何实现跨语言**

&lt;img width='1612' height='673' alt='Image' src='https://github.com/user-attachments/assets/c35b7071-f6c2-4bee-9457-f971ac5e7438' /&gt;

CLS(common language specification)，公共语言规范
只要所有语言都遵守 CLS 规则，它们写出来的代码编译后都能变成中间语言（IL），所以可以互相调用

CLI (Common Language Infrastructure)，公共语言基础结构
整个 .NET运行体系标准，国际工业标准

CTS (Common Type System)，公共类型系统
所有 .NET 语言必须遵守的“数据类型统一规则”，CLS是CTS的子级
int 在所有语言中必须统一：
System.Int32
string 必须统一：
System.String
bool 必须统一：
System.Boolean

**如何实现跨平台**
**.Net Framework框架** 2002
主要用于开发window下的应用程序，部分开源

&lt;img width='1704' height='793' alt='Image' src='https://github.com/user-attachments/assets/0d997240-c80c-40fd-ae4a-5258140deca2' /&gt;
不同语言使用类库--&gt;编译--&gt;生成程序集(.dll(IL 元数据 资源) .exe)--&gt;CLR(负责运行管理)--&gt;JIT(在CLR里面，翻译机器码)--&gt;window

CLR(common language runtime)，公共语言进行时
微软写好的代码，用来管理我们写的代码。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/guan-yu-.Net.html</guid><pubDate>Thu, 02 Jul 2026 19:06:40 +0000</pubDate></item><item><title>音乐音效设置的制作</title><link>https://linarisx.github.io/lynn.github.io/post/yin-le-yin-xiao-she-zhi-de-zhi-zuo.html</link><description># 思路
制作好音乐数据和面板
之后再设置页面逻辑

## 数据管理类单例模式
统一来管理数据的存储读取

## 背景音乐挂在空对象，并且需要一个脚本来设置音量的大小
### 初始化音量
游戏一开始就设置好音量，数据来自数据管理类（也就是之前存储的）
### 方法提供给面板快捷设置（制作成单例模式脚本）

1. 设置音量大小
2. 是否开启

## 在面板通过控件
### 初始化空间的音量

1. 一点开面板就设置好控件大小，数据来自数据管理类（也就是之前存储的）
2. 设置背景音乐的大小
3. 更改数据
4. 保存数据（统一在某个按键存储）。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/yin-le-yin-xiao-she-zhi-de-zhi-zuo.html</guid><pubDate>Wed, 06 May 2026 07:45:28 +0000</pubDate></item><item><title>Json</title><link>https://linarisx.github.io/lynn.github.io/post/Json.html</link><description># Json
## JsonUtility
&lt;img width='374' height='70' alt='Image' src='https://github.com/user-attachments/assets/01ee836f-44ba-4364-9e24-405d1d052036' /&gt;

### 序列化
**`JsonUtility.ToJson(data)`**
把内存中的数据存储到硬盘上

把一串类对象的数据序列化为Json字符串
这个Json字符串可以写入硬盘中

&lt;img width='556' height='353' alt='Image' src='https://github.com/user-attachments/assets/604c9d0e-1209-4514-bff1-edaef08cb80c' /&gt;


&lt;img width='432' height='117' alt='Image' src='https://github.com/user-attachments/assets/27a830e0-ad4d-4f96-836e-2f9266871001' /&gt;

### 反序列化
**`JsonUtility.FromJson&lt;FireInfo&gt;(字符串);`**
把硬盘的数据读取到内存中

读取文件中的Json字符串
```c#
string readFileInfo  = File.ReadAllText(Application.streamingAssetsPath + '/FireData2.json');
print(readFileInfo);//正常读取
```
使用Json字符串内容 反序列化转换成类对象
`FireInfo fireData = JsonUtility.FromJson&lt;FireInfo&gt;(readFileInfo);`

注意必须是对象，如果想直接反序列数据结构得把这个数组再包裹一层对象
注意json文件必须是UTF-8

## LitJson
### 序列化
```c#
string strJson = JsonMapper.ToJson(p);
File.WriteAllText(Application.persistentDataPath + '/' + fileName + '.json', strJson);//写进json文件
```

### 反序列化
```c#
string strJson = File.ReadAllText(Application.persistentDataPath + '/' + fileName + '.json');
JsonMapper.ToObject&lt;PlayerInfo2&gt;(strJson);
```
LitJson虽然支持字典但是
注意键如果是int类型，会出问题，因为json里面默认转成字符串，再次读取数据之后反序列化会出问题因为无法创建int类型的键

&lt;img width='373' height='92' alt='Image' src='https://github.com/user-attachments/assets/33181a4f-c354-4cf7-9bd3-62f9ecf0d18f' /&gt;

&lt;img width='592' height='171' alt='Image' src='https://github.com/user-attachments/assets/9de68222-5854-4c17-8d8b-cae22ce102db' /&gt;。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/Json.html</guid><pubDate>Sun, 03 May 2026 10:30:23 +0000</pubDate></item><item><title>【unity基础】射线检测</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-she-xian-jian-ce.html</link><description># 射线检测
## 射线的定义
### 定义一个3d射线，（原点，方向）
```c#
Ray r = new Ray(Vector3.zero, Vector3.forward);
print(r.origin);//原点
print(r.direction);//方向
```

### 摄像机发出的射线（屏幕发射）
`Ray r2 = Camera.main.ScreenPointToRay(Input.mousePosition);
`
## 射线检测
`Ray r3 = new Ray(Vector3.zero, Vector3.forward);`
### 传入一个射线
返回值bool 判断是否有检测到对象
（射线，检测距离，层级，是否忽视碰撞器）
```c#
if (Physics.Raycast(r3, 1000, 1 &lt;&lt; LayerMask.NameToLayer('Monster'), QueryTriggerInteraction.UseGlobal))
{
    print('检测到');
}
```
### 直接传入起点和方向
```c#
if (Physics.Raycast(Vector3.zero, Vector3.forward, 1000, 1 &lt;&lt; LayerMask.NameToLayer('Monster'), QueryTriggerInteraction.UseGlobal))
{
    print('检测到');
}
```

## 获取检测到的对象信息
### 传入射线
物体信息
`RaycastHit rayInfo;`
（射线， out进去赋值传出来， 距离， 层级， 是否检测碰撞器）
```c#
if (Physics.Raycast(r3, out rayInfo, 1000, 1 &lt;&lt; LayerMask.NameToLayer('Monster'), QueryTriggerInteraction.UseGlobal))
{
    print('碰撞到物体检测到信息');

    print('碰撞到物体的名字' + rayInfo.collider.gameObject.name);

    print('碰撞到物体的点' + rayInfo.point);

    //射到的点和平面的法线
    print('法线信息' + rayInfo.normal);

    print('碰撞的对象位置' + rayInfo.transform.position);

    print('碰撞的对象离射线起点的距离' + rayInfo.distance);
    //时间 = 距离 / 速度
    //可以计算自由下落的距离 = 时间 * 重力加速度
}
```
### 直接传入起点和方向
`if (Physics.Raycast(Vector3.zero, Vector3.forward, out rayInfo, 1000, 1 &lt;&lt; LayerMask.NameToLayer('Monster'), QueryTriggerInteraction.UseGlobal)) { }`

## 获取相交的多个物体对象(穿透)
### 传入
```c#
RaycastHit[] objs = Physics.RaycastAll(r3, 1000, 1 &lt;&lt; LayerMask.NameToLayer('Monster'), QueryTriggerInteraction.UseGlobal);
for (int i = 0; i &lt; objs.Length; i++)
{
    print('碰到的物体名' + objs[i].collider.gameObject.name);
}
```
### 直接传入起点和方向
`RaycastHit[] objs2 = Physics.RaycastAll(Vector3.zero, Vector3.forward, 1000, 1 &lt;&lt; LayerMask.NameToLayer('Monster'), QueryTriggerInteraction.UseGlobal);`

## 返回碰撞的数量
```c#
if (Physics.RaycastNonAlloc(r3, objs, 1000, 1 &lt;&lt; LayerMask.NameToLayer('Monster'), QueryTriggerInteraction.UseGlobal) &gt; 0)
{
    print('碰撞数量');
}
```。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-she-xian-jian-ce.html</guid><pubDate>Sat, 02 May 2026 10:29:34 +0000</pubDate></item><item><title>【unity基础】范围检测</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-fan-wei-jian-ce.html</link><description># 范围检测
用于检测一个范围，一般瞬时检测
## 立方体检测
参数：
（中心点，立方体长宽高大小(以中心点向各个方向延伸的距离)，旋转角度，层级(默认检测所有层)，是否忽略检测触发器(默认全局)）
```c#
Collider[] colliders = Physics.OverlapBox(Vector3.zero, Vector3.one * 0.5f,
                       Quaternion.identity,
                       1 &lt;&lt; LayerMask.NameToLayer('UI') | 1 &lt;&lt; LayerMask.NameToLayer('Default'),
                       QueryTriggerInteraction.UseGlobal);
for (int i = 0; i &lt; colliders.Length; i++)
{
    //这里的colliders是得到该对象身上的碰撞器
    print('检测到' + colliders[i].gameObject.name);
}
```

### 检测碰撞器数量
第三个参数传个数组接收检测到的碰撞器
```c#
if (Physics.OverlapBoxNonAlloc(Vector3.zero, Vector3.one * 0.5f, colliders) != 0)
{

}
```
## 球形检测
参数：中心点，半径
```c#
Physics.OverlapSphere(Vector3.zero, 5, 1 &lt;&lt; LayerMask.NameToLayer('Default'), QueryTriggerInteraction.UseGlobal);
if (Physics.OverlapSphereNonAlloc(Vector3.zero, 5, colliders) != 0) { }
```

## 胶囊体检测
参数：第一个圆的中心点，第二个圆的中心点，半径
```c#
Physics.OverlapCapsule(Vector3.zero, Vector3.forward, 2);
if(Physics.OverlapCapsuleNonAlloc(Vector3.zero, Vector3.forward,2, colliders) != 0) { }
```

## 层级相关内容
左移 代表32位中每一位都是一个层级编号，刚好int就是32位，一共有32个层级
编号 -&gt; 1左移n位
0 —— 1 &lt;&lt; 0000 0000 0000 0000 0000 0000 0000 0001 = 1 
1 —— 1 &lt;&lt; 0000 0000 0000 0000 0000 0000 0000 0010 = 2
2 —— 1 &lt;&lt; 0000 0000 0000 0000 0000 0000 0000 0100 = 4 
3 —— 1 &lt;&lt; 0000 0000 0000 0000 0000 0000 0000 1000 = 8 
4 —— 1 &lt;&lt; 0000 0000 0000 0000 0000 0000 0001 0000 = 16 
5 —— 1 &lt;&lt; 0000 0000 0000 0000 0000 0000 0010 0000 = 32 

### 表示同时检测这两层层级
`1 &lt;&lt; LayerMask.NameToLayer('UI') | 1 &lt;&lt; LayerMask.NameToLayer('Default'),`
0010 0000 = 32
0000 0001 = 1
**进行或(|)运算 有一则一**
计算结果 0010 0001 = 33 //代表两层
unity内部将每一层与0010 0001进行与(&amp;)运算 同为1就取1
只要结果不为0，那就代表有这一层

### 检测除了该层以外的所有层

1. 用该层进行异或位运算(**异或 ^ 相同为0 不同为1**)
`LayerMask mask = ~0 ^ (1 &lt;&lt; LayerMask.NameToLayer('Default'));`//~0：32个1，代表检测所有层
1111 1111 1111 1111 1111 1111 1111 1111//~0
0000 0000 0000 0000 0000 0000 0000 0001//1 &lt;&lt; LayerMask.NameToLayer('UI'))
1111 1111 1111 1111 1111 1111 1111 1110//异或的结果

2 直接取反 
`mask = ~(1 &lt;&lt; LayerMask.NameToLayer('Default'));`。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-fan-wei-jian-ce.html</guid><pubDate>Sat, 02 May 2026 10:10:57 +0000</pubDate></item><item><title>【unity基础】LineRenderer组件</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91LineRenderer-zu-jian.html</link><description># 使用方法
## 新建空物体并挂在LineRenderer组件
GameObject obj = new GameObject();
obj.name = 'line';
LineRenderer lineRenderer = obj.AddComponent&lt;LineRenderer&gt;();

## 首尾相连
lineRenderer.loop = true;

## 开始结束宽
`lineRenderer.startWidth = 0.2f;`
`lineRenderer.endWidth = 1;`

## 开始结束颜色
`lineRenderer.startColor = Color.red;`
`lineRenderer.endColor = Color.white;`

## 设置材质
```c#
Material material = Resources.Load&lt;Material&gt;('M');
lineRenderer.material = material;
```

## 设置位置
不设置默认为000

### 点的个数
`lineRenderer.positionCount = 4;`
### 设置每个点的位置
```c#
lineRenderer.SetPositions(new Vector3[]
{ new Vector3(0, 0, 0) ,
  new Vector3(0, 0, 5),
  new Vector3(5, 0, 5),
   }
);
```
### 设置单个点的位置 （索引3的点的位置）
`lineRenderer.SetPosition(3, new Vector3(5, 0, 0));`

## 是否使用世界坐标系
`lineRenderer.useWorldSpace = true;`

## 是否受光源影响
`lineRenderer.generateLightingData = true;`

## 注意
一个物体只能有一个LineRenderer。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91LineRenderer-zu-jian.html</guid><pubDate>Sat, 02 May 2026 09:52:36 +0000</pubDate></item><item><title>【unity基础】场景异步加载</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-chang-jing-yi-bu-jia-zai.html</link><description># 场景异步加载
## 场景同步加载
`SceneManager.LoadScene('GameScene');`
缺点：切换场景是会删除当前场景的所有对象，，并且去加载下一个场景的相关信息
如果当前场景对象过多或者下一个场景对象过多，会耗时卡顿

## 事件回调函数加载
```c#
AsyncOperation ao = SceneManager.LoadSceneAsync('Lesson20_Test');
ao.completed += (a) =&gt;//切场景时被删的对象内存依然存在，相对于事件存了函数引用，在GC时这个事件不会被释放，所以才可以一直监听
{
    print('加载完成');
    //可以自己做地图编辑器，地图编辑器产生的配置文件动态的创建这些资源
};
```

## 协程异步加载（可以在加载中执行一些逻辑）
对象删除/失活，脚本失活协程不会继续执行
只要异步加载成功后面的代码都执行不了
所以要让该对象过场景不被删除
```c#
DontDestroyOnLoad(this.gameObject);
StartCoroutine(LoadScene('Lesson20_Test'));
```
```c#
IEnumerator LoadScene(string name)
{
    AsyncOperation ao = SceneManager.LoadSceneAsync(name);
    print('加载中');
    yield return ao;
    //过场景不被删除才能有下一步
    //加载完执行
    print('加载完');
}
```
```c#
IEnumerator LoadScene(string name)
{
 AsyncOperation ao = SceneManager.LoadSceneAsync(name);
    while (!ao.isDone)//没结束
    {
        print(ao.progress);//打印进度（0~1） 不准确 不常用
        yield return null;
        //做一些进度条
    }
    循环结束，场景加载结束，进度条顶满，隐藏进度条
}
//第二种 自定义进度条规则
yield return ao;
//场景加载结束 进度条20%
//加载怪物 40%
//动态加载 场景模型 100%
//隐藏进度条
```。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-chang-jing-yi-bu-jia-zai.html</guid><pubDate>Sat, 02 May 2026 09:30:26 +0000</pubDate></item><item><title>【unity基础】Resources的加载与卸载</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91Resources-de-jia-zai-yu-xie-zai.html</link><description># Resources的加载与卸载

## 同步加载
### 同步加载的不同方式
`Object obj = Resources.Load('Cube');`
`TextAsset ta = Resources.Load&lt;TextAsset&gt;('TextAsset/Test');`
`AudioClip ad = Resources.Load('Audio/LaserShoot') as AudioClip;`
`Sprite tx =  Resources.Load('Texture/2026_4.9', typeof(Sprite)) as Sprite;`

### 支持格式

1. 预设体对象-GameObject
2. 音效文件-AudioClip
3. 文本文件-TextAsset
4. 图片文件-Texture

### 文本类型
```c#
TextAsset ta = Resources.Load&lt;TextAsset&gt;('TextAsset/Test');
print(ta.text);
print(ta.bytes);//字节数据组
```

### 音效文件
```c#
AudioClip ad = Resources.Load('Audio/LaserShoot') as AudioClip;
audioS.clip = ad;
audioS.Play();
```

### 加载同名的所有资源
```c#
Object[] objs = Resources.LoadAll('Texture/Test');
foreach(Object item in objs)
{
    if(item is Sprite)
    {

    }
    else if(item is TextAsset)
    {

    }
}
```
## 异步加载
### 解决的问题
硬盘把数据读取到内存中是需要计算的，如果加载的资源过大会导致卡顿
一步加载中，通过后台新开线程同步加载，解决卡顿

### 通过后台线程异步加载
ResourceRequest的父类有个监听事件
```c#
ResourceRequest rq = Resources.LoadAsync&lt;Texture&gt;('Texture/2026_4.9');
rq.completed += IsOver;//如果加载完成，就在内部invoke执行isOver
//rq.asset 一定要加载完才使用不要在这里！
print(Time.frameCount);//帧

void IsOver(AsyncOperation ac)
{
    print('加载完成');
    tex  = (ac as ResourceRequest).asset as Texture;
    print(Time.frameCount);//帧
}
```
### 通过协程异步加载
```c#
StartCoroutine(Load());
IEnumerator Load()
{
   ResourceRequest rq = Resources.LoadAsync&lt;Texture&gt;('Texture/2026_4.9');
   print(Time.frameCount);
   yield return rq;//unity自己会判断rq是在异步加载  WaitForSeconds基类和ResourceRequest基类的基类是一样的

   //自己判断如果加载完就执行后面的内容
   tex = rq.asset as Texture;
   print(Time.frameCount);//至少一帧才能使用资源  
}
```
```c#
IEnumerator Load()
{
   ResourceRequest rq = Resources.LoadAsync&lt;Texture&gt;('Texture/2026_4.9');
   while (!rq.isDone)//没有加载完毕进入循环
     {
         print(rq.progress);//加载进度0~1
         yield return null;//下一帧执行
     }
     tex = rq.asset as Texture;//加载结束再使用来绘制
}
```
## 其他
加载资源第一次就存到内存中了
重复加载同个资源不消耗内存，消耗性能
多个resources是可以的，打包时自动合并成一个

## 资源卸载
### 注意
不能释放GameObject对象，因为会用于实例化对象
```c#
GameObject obj = Resources.Load&lt;GameObject&gt;('Cube');
Resources.UnloadAsset(obj);//不可行，没有实例化的也不行
```

### 卸载未使用的资源
切场景时和GC一起
```c#
Resources.UnloadUnusedAssets();
GC.Collect();//调用GC
```

### 卸载方式
```c#
if (Input.GetKeyDown(KeyCode.Alpha1))
{
    print('加载');
    texture = Resources.Load&lt;Texture&gt;('Texture/2026_4.9');
}
else if (Input.GetKeyDown(KeyCode.Alpha2))
{
    print('卸载');
    Resources.UnloadAsset(texture);
    texture = null;//像引用指针一直指着那个资源地址，所以要置空
}
```。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91Resources-de-jia-zai-yu-xie-zai.html</guid><pubDate>Sat, 02 May 2026 09:10:04 +0000</pubDate></item><item><title>【unity基础】特殊文件</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-te-shu-wen-jian.html</link><description>## 工程路径
实际上就是Assets文件夹
**`Application.dataPath`**
只会在编辑时使用
游戏打包，该路径不存在

## Resources资源文件夹
**`Application.dataPath + '/Resources'`**
一般用API加载
动态加载资源放在里面
打包时文件夹内容会都打包出去，unity自动加密
打包后只能读

## streamingAssetsPath
不同平台得到的路径都是不一样的，所以不要拼接写
**`Application.streamingAssetsPath`**
移动平台只读，PC可读可写
打包出去不会被加密
可以放入自定义动态加载的初始资源（热更新，配置文件等）

## persistentDataPath
**`print(Application.persistentDataPath);`**
所有平台可读可写
固定数据文件夹
保存玩家数据，热更新下载资源，代码
放动态下载 / 创建的文件，游戏中创建 / 获取的文件

## Plugins文件夹
放插件

## Editor
放编辑器相关(一般不获取，可通过路径) 内容不会被打包出去

## Standard Assets
下载unity自带资源会自动创建，自己创建。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-te-shu-wen-jian.html</guid><pubDate>Thu, 30 Apr 2026 11:05:07 +0000</pubDate></item><item><title>【unity基础】协同程序</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-xie-tong-cheng-xu.html</link><description># 协同程序
## 基本使用
### 开启协程
`StartCoroutine(MyCoroutine(1, '小林'));`
`IEnumerator ie = MyCoroutine(1, '小林');`

可以一次开多个
```c#
Coroutine c1 = StartCoroutine(MyCoroutine(1, '小林'));
Coroutine c2 = StartCoroutine(MyCoroutine(1, '小林'));
Coroutine c3 = StartCoroutine(MyCoroutine(1, '小林'));
```

### 关闭所有协程
`StopAllCoroutines();`

### 关闭一个指定协程
`StopCoroutine(c1);
`
### 协程相关代码
```c#
IEnumerator MyCoroutine(int i, string str)
{
  print(i);
  yield break;//跳出协程（相当于停止了）
  print(i);
  yield return new WaitForSeconds(2);//等2秒执行下部分
  print(str);
  yield return new WaitForSeconds(2);//等2秒执行下部分
  print('2');
  yield return new WaitForSeconds(2);//等2秒执行下部分
  print('3');

  //可以写循环
  while (true)
  {
      print('5');
      yield return new WaitForSeconds(1);//每隔一秒打印一次5（每个一秒执行一次这个循环）
  }
}

```
### 类型

1. 等2秒执行下部分
yield return new WaitForSeconds(2);
在Update和LateUpdate之间执行

2. 下一帧执行
yield return null; /yield return 数字;
在Update和LateUpdate之间执行

3. 等待下一个固定物理帧更新时执行
yield return new WaitForFixedUpdate();
在FixedUpdate和碰撞检测相关函数之后执行

4. 等待摄像机和GUI渲染完成后执行
yield return new WaitForEndOfFrame();

5. 在LateUpdate之后的渲染相关处理完毕后执行
yield return new WaitForEndOfFrame();
截图

6. 一些特殊类型的对象，比如异步加载相关函数返回的对象
一般在Update和LateUpdate之间执行

### 注意
挂载的对象和脚本被删除，失活对象，协程不执行
失活脚本协程仍然执行

## 协程原理
迭代器
```c#
IEnumerator ie = MyCoroutine();
print(ie.Current); // null
ie.MoveNext(); // 1
print(ie.Current); //11
ie.MoveNext();
print(ie.Current); //12
ie.MoveNext();
print(ie.Current); //Test;
Test t = ie.Current as Test;
print(t.time); //10

while (ie.MoveNext())//返回true代表还有下一个值
{
   print(ie.Current);
}
```
```c#
IEnumerator MyCoroutine()
{
   print('1');
   yield return 11;
   yield return 12;
   yield return new Test(10);
}
```。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-xie-tong-cheng-xu.html</guid><pubDate>Thu, 30 Apr 2026 10:42:58 +0000</pubDate></item><item><title>【unity基础】延迟函数</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-yan-chi-han-shu.html</link><description># 延迟函数
## 延迟函数
### 延迟两秒执行
`Invoke('Delay', 2);`
### 第一次两秒后执行，之后每一秒执行一次
`InvokeRepeating('RepeatingDaley', 2, 1);`

延迟函数只能是无参或该类里的函数，其他得包裹才能执行

## 取消延迟函数
取消没有执行的延迟函数，不会报错
### 取消所有延迟函数
`CancelInvoke();`

### 取消指定延迟函数
同名全部一起取消
`CancelInvoke('Delay');
`
### 判断是否有延迟函数
```c#
if (IsInvoking())
{
    print('存在延迟函数');
}
if (IsInvoking('RepeatingDaley'))
{
    print('存在延迟函数RepeatingDaley');
}
```

## 注意
失活该脚本挂载的对象和 该脚本，延迟函数仍然执行
删除脚本和该对象，延迟函数不再执行

所以可以通过生命周期函数来控制失活时的延迟函数是否执行
```c#
private void OnEnable()
{
   InvokeRepeating('RepeatingDaley', 2, 0.5f);
}
private void OnDisable()
{
   CancelInvoke('RepeatingDaley');
}
```。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-yan-chi-han-shu.html</guid><pubDate>Thu, 30 Apr 2026 10:21:05 +0000</pubDate></item><item><title>摄像机跟随玩家方式</title><link>https://linarisx.github.io/lynn.github.io/post/she-xiang-ji-gen-sui-wan-jia-fang-shi.html</link><description>## 通过当玩家的子对象
## 通过坐标的偏移位置计算
摄像机的移动需要写在LateUpdate
因为update和lateUpda之间有一些渲染动画要处理
```c#
public Transform target;
public float yOffect = -4;
public float zOffect = 7;
void LateUpdate()
{
      this.transform.position = target.position + target.forward * yOffect + target.up * zOffect;
      this.transform.LookAt(target);
}
```
## 通过插值计算

&lt;img width='431' height='197' alt='Image' src='https://github.com/user-attachments/assets/e60e8558-625c-4bfb-a393-a289a5f3c43d' /&gt;

Vector3.Lerp跟随玩家
Quaternion.Slerp看向玩家。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/she-xiang-ji-gen-sui-wan-jia-fang-shi.html</guid><pubDate>Thu, 30 Apr 2026 09:59:06 +0000</pubDate></item><item><title>【unity基础】四元数</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-si-yuan-shu.html</link><description># 四元数
## 为何使用四元数
四元数可以解决的问题
普通旋转会出现万向节死锁，也就是三位变成只有二维旋转
一个物体方向能有多个欧拉角角度 比如90,450,等等

## 四元数的使用

&lt;img width='265' height='169' alt='Image' src='https://github.com/user-attachments/assets/2d22f2e6-b661-46c9-9bc3-f0306c3b9ff6' /&gt;

&lt;img width='492' height='251' alt='Image' src='https://github.com/user-attachments/assets/afaaf22d-78a2-4143-b537-7f03f3e2e620' /&gt;


###  四元数初始化

&lt;img width='383' height='181' alt='Image' src='https://github.com/user-attachments/assets/e5d18617-fe1e-4b10-bdcd-f15cc8302e07' /&gt;

`Quaternion q = new Quaternion(0, Mathf.Sin(30 * Mathf.Deg2Rad) * 1, 0, Mathf.Cos(30 * Mathf.Deg2Rad));`
**`Quaternion q = Quaternion.AngleAxis(60, Vector3.up);`**

```c#
GameObject obj = GameObject.CreatePrimitive(PrimitiveType.Cube);
obj.transform.rotation = q;
```

### 四元数欧拉角的转换
欧拉转四元
```c#
Quaternion q2 = Quaternion.Euler(60, 0, 0);
GameObject obj2 = GameObject.CreatePrimitive(PrimitiveType.Cube);
obj2.transform.rotation = q2;
```
四元转欧拉
`print(q2.eulerAngles);`

## 四元数的常用用法
### 单位四元数
旋转角度(0,0,0) 标量1/-1
`print(Quaternion.identity);`

### 插值运算
让一个物体旋转成跟目标物体一样的角度
**1. 缓速**
`A.rotation = Quaternion.Slerp(A.rotation, target.transform.rotation, Time.deltaTime);`
**2. 匀速**
```
time += Time.deltaTime;
B.rotation = Quaternion.Slerp(startB, target.transform.rotation, time);
```

### LookRotation
传入一个向量，让一个物体旋转至那个向量的方向，以至于一直看向那个物体
返回的是一个旋转角度
`C.rotation = Quaternion.LookRotation(D.position - C.position);`

## 四元数的计算
### 旋转
相对于自己坐标系旋转30度
`this.transform.rotation *= Quaternion.AngleAxis(60, Vector3.up);`

### 旋转向量
旋转后的向量 = 四元数 * 向量
```c#
Vector3 v = Vector3.forward;
v = Quaternion.AngleAxis(45, Vector3.up) * v; 
```。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-si-yuan-shu.html</guid><pubDate>Thu, 30 Apr 2026 09:44:52 +0000</pubDate></item><item><title>Debug相关</title><link>https://linarisx.github.io/lynn.github.io/post/Debug-xiang-guan.html</link><description># Debug
不用继承MonoBehaviour

## 打印
`Debug.Log('打印的信息');`

`Debug.LogError('出错误了！');`

`Debug.LogWarning('警告！');`

## 绘制射线 线段
画射线 起点 方向 颜色
`Debug.DrawRay(this.transform.position, this.transform.forward, Color.red);`

画线段 起点 终点 颜色
`Debug.DrawLine(this.transform.position, target.transform.position, Color.red);`。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/Debug-xiang-guan.html</guid><pubDate>Thu, 30 Apr 2026 07:34:40 +0000</pubDate></item><item><title>【unity基础】向量</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-xiang-liang.html</link><description># 向量模长和单位向量
## 基础概念

1. 两点之间得到向量
2. 一个点可以是和原点的向量

**向量模长：两点距离，向量长度
单位向量：单位为1的方向线路，与移动相关**

模长计算公式：根号下(x² + y² + z²)
单位向量计算：（x/模长， y/模长， z/模长）

## 代码相关
可以是点 也可以是向量
`Vector3 C = new(1,2,3);`

```c#
Vector3 A = new(1,7,3);
Vector3 B = new(4,2,3);
Vector3 AB = B - A;
Vector3 BA = A - B;
```

AB模长
`print(AB.magnitude);`

AB距离
`print(Vector3.Distance(A, B));`

OC模长（原点到C的模长）
`print(C.magnitude);`

AB单位向量
`print(AB.normalized);`

单位向量=向量/模长
`print(AB / AB.magnitude);`

# 向量的加减乘除
## 概念
向量 + 向量 = 向量（首尾相连连首尾）
位置 + 向量 = 位置（平移位置）

位置 - 位置 = 向量（终点 - 起点）
向量 - 向量 = 向量（头连头，尾指尾）
位置 - 向量 = 位置（平移位置）

向量乘除：用于模长的放大缩小

```c#
this.transform.localScale *= 2;
this.transform.localScale /= 2;
```

# 向量的点乘
## 基本概念
向量点乘 ： 另一个向量在该向量上的投影长度
公式：A·B = Xa * Xb + Ya * Yb + Za * Zb

点乘结果 &gt; 0 夹角为锐角
点乘结果 = 0 夹角为直角
点乘结果 &lt; 0 夹角为钝角

可以通过点乘结果来判断敌人是否在玩家前方
```c#
 result = Vector3.Dot(this.transform.forward, (target.position - this.transform.position));
 if(result &gt;= 0)
 {
     print('敌人在前方');
 }
 else
 {
     print('敌人在后方');
 }
```

# 计算两个向量之间的角度
## 公式：
β = Acos(A单位向量 点乘 B单位向量) 0 - 180
## 代码：
```c#
result = Vector3.Dot(this.transform.forward, (target.position - this.transform.position).normalized);
print(Mathf.Acos(result) * Mathf.Rad2Deg);
```
另一种写法
`Vector3.Angle(this.transform.position, target.position - this.transform.position);`

# 叉乘
## 基本概念
A叉乘B得到一个新向量（新向量是AB平面的法向量）
X = YaZb - ZaYb   Y = ZaXb - XaZb   Z = XaYb - YaXb

&lt;img width='609' height='273' alt='Image' src='https://github.com/user-attachments/assets/9ccf50db-bc4d-4bd5-8027-46f8596dca28' /&gt;

## 几何意义
这个新向量同时垂直于AB
A×B = -(B×A)

A×B
判断的是向量
y &gt; 0 B在A右侧
y &lt; 0 B在A左侧

&lt;!-- Failed to upload 'Snipaste_2026-04-30_15-46-59.png' --&gt;
&lt;img width='719' height='332' alt='Image' src='https://github.com/user-attachments/assets/0875a201-9fdc-4ba7-8def-6c17033bf2b7' /&gt;

## 代码
```c#
Vector3 C = Vector3.Cross(A.position, B.position);//叉乘
if(C.y &gt; 0)
{
 print('B在A右侧');
}
else
{
 print('B在A左侧');
}
```

# 向量插值运算
## 基本概念
让一个位置移动到另一个位置（缓速/匀速）
result = start + (end-start)*t

和Mathf的区别：
Mathf.lerp是一个数字(二维)
vector3.lerp是向量(三位)

## 代码
### 缓速
`A.position = Vector3.Lerp(A.position, target.position, Time.deltaTime);`

### 匀速
当time&gt;=1时，相当于与目标重合，或超出终点
```c#
if(nowTarget != target.position)//target改变了就会进这里
{           
    nowTarget = target.position;
    time = 0;
    startPos = B.position;
}
time += Time.deltaTime;
B.position = Vector3.Lerp(startPos, nowTarget, time);
```

### 球形差值
弧形轨迹（第三个参数区间是0~1的值）
`C.position = Vector3.Slerp(Vector3.right*10, Vector3.forward*10, time*0.1f);`。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-xiang-liang.html</guid><pubDate>Thu, 30 Apr 2026 07:10:06 +0000</pubDate></item><item><title>【unity基础】三角函数</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-san-jiao-han-shu.html</link><description># 三角函数
## 三角
### 角度转弧度
(30° = π/180° * 30 = π/6) 
π/6 -&gt; 30° - &gt; sin30° = 0.5
`Mathf.Sin(30 * Mathf.Deg2Rad)`

π/3 -&gt; 60° -&gt; cos60° = 0.5
`Mathf.Cos(60 * Mathf.Deg2Rad)`

## 反三角
### 弧度转角度
默认算出的是弧度制
0.5 -&gt; 30的弧度制 -&gt; 30°
```c#
float s = Mathf.Asin(0.5f) 
print(s * Mathf.Rad2Deg);
```

0.5 -&gt; 60的弧度制 -&gt; 60°
```c#
float c = Mathf.Acos(0.5f);
print(c * Mathf.Rad2Deg);
```。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91-san-jiao-han-shu.html</guid><pubDate>Thu, 30 Apr 2026 04:49:15 +0000</pubDate></item><item><title>【unity基础】Mathf</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91Mathf.html</link><description># Mathf
## 基础常见使用
### 绝对值
`print(Mathf.Abs(-1));`

### 向上取整
`print(Mathf.CeilToInt(1.3f));`

### 向下取整
`print((int)1.3f);`
`print(Mathf.FloorToInt(1.3f));`

### clamp 夹紧 
比最小还小就输出最小，比最大还大就输出最大，其他就是正常值
（值，最小值，最大值）
`print(Mathf.Clamp(10, 11, 20));`

### 最大值
`print(Mathf.Max(12, 6, 5));`
### 最小值
`print(Mathf.Min(13, 7, 4));`

### 四舍五入
`print(Mathf.RoundToInt(1.3f));`
`print(Mathf.RoundToInt(1.6f));`

### 平方根，开根号
开不出整数会自动算小数
`print(Mathf.Sqrt(64));`
`print(Mathf.Sqrt(10));`

### 判断是否是2的次方
返回true/false
`print(Mathf.IsPowerOfTwo(10));`

### 判断正负数
正数返回1，负数返回-1
`print(Mathf.Sign(10));`
`print(Mathf.Sign(-10));`

## 线性差值的运算
Lerp 线性插值运算 一直计算
result = Mathf.Lerp(start, end, t);
result = start + (end - start) * t
t为插值系数（0~1）

```c#
float start = 0;
float result = 0;
float time = 0;
void Update()
{   
   start = Mathf.Lerp(start, 10, Time.deltaTime);//一直趋近10，start变大

   time += Time.deltaTime;
   result = Mathf.Lerp(start, 10, time);//一直匀速上涨到10，因为t一直变大
}
```
### 其他
如果使用匀速移动到目标点，需要一直更新开始位置、目标位置、时间。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ji-chu-%E3%80%91Mathf.html</guid><pubDate>Thu, 30 Apr 2026 04:23:29 +0000</pubDate></item><item><title>【unity入门】场景切换 游戏退出 光标 随机数 委托</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-chang-jing-qie-huan-%20-you-xi-tui-chu-%20-guang-biao-%20-sui-ji-shu-%20-wei-tuo.html</link><description># 场景切换 游戏退出 光标 随机数 委托
## 场景切换和游戏退出
### 改变场景
`SceneManager.LoadScene('Scene2');`

### 退出游戏
`Application.Quit();`

## 隐藏鼠标改变图片
`public Texture2D txt;`

Escape可以退出

### 隐藏光标
`Cursor.visible = false;`

### 上锁隐藏 鼠标，位置屏幕中间
隐藏鼠标
`Cursor.lockState = CursorLockMode.None;`
上锁鼠标
`Cursor.lockState = CursorLockMode.Locked;`

### 限制鼠标在屏幕范围
`Cursor.lockState = CursorLockMode.Confined;`

### 设置光标图片
（图片，相对于鼠标图片左上角的位置，自动模式(一般不更改)）
`Cursor.SetCursor(txt, Vector2.zero, CursorMode.Auto);`

## 随机数和unity自带委托
### 随机数
Unity
整数 左包含右不包含
`int i = Random.Range(0, 101);`
小数 左右都包含
`float f = Random.Range(1.1f, 9.9f);`

C#
```
System.Random r = new System.Random();
r.Next(0, 101);
```
Random在两个命名空间都有，所以只能指明

### 委托
Unity 
只有无返回值的
```c#
UnityAction ac = () =&gt; { };
UnityAction&lt;int&gt; ac1 = (int i) =&gt; { };
```

C#
```c#
//无参无返回值
System.Action action = () =&gt; { };
//有参无返回值
System.Action&lt;int&gt; action1 = (int i) =&gt; { };

//无参有返回值
System.Func&lt;string&gt; fun = () =&gt; { return ''; };
//有参有返回值&lt;参数类型，返回值类型&gt;
System.Func&lt;string, int&gt; func = (string i) =&gt; { return 1; };
```。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-chang-jing-qie-huan-%20-you-xi-tui-chu-%20-guang-biao-%20-sui-ji-shu-%20-wei-tuo.html</guid><pubDate>Wed, 29 Apr 2026 10:04:53 +0000</pubDate></item><item><title>制作小地图</title><link>https://linarisx.github.io/lynn.github.io/post/zhi-zuo-xiao-di-tu.html</link><description>1. 新建一个摄像机，俯视地面
2. 创建Render Texture
3. 将此贴图拖给摄像机上的Target Texture
4. 摄像机的视角就会出现在贴图中
5. 把贴图托给UI。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/zhi-zuo-xiao-di-tu.html</guid><pubDate>Wed, 29 Apr 2026 09:44:31 +0000</pubDate></item><item><title>【unity入门】AudioSource音频相关</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91AudioSource-yin-pin-xiang-guan.html</link><description># AudioSource音频相关
## 音频切片
```c#
AudioSource audioSource;
public GameObject obj;
public AudioClip clips;
void Start()
{
   audioSource = GetComponent&lt;AudioSource&gt;();
   //Instantiate(obj);
   AudioSource aus = this.AddComponent&lt;AudioSource&gt;();
   aus.clip = clips;
   aus.Play();
}
```

## 音频的播放与暂停
```c#
if(Input.GetKeyDown(KeyCode.P))
{
    //播放音频
    audioSource.Play();
    //延迟五秒播放
    //audioSource.PlayDelayed(5);
}
if(Input.GetKeyDown(KeyCode.S))
{
    //停止音频
    audioSource.Stop();
}
if(Input.GetKeyDown (KeyCode.Space))
{
    //暂停
    audioSource.Pause();
}
if (Input.GetKeyDown(KeyCode.X))
{
    //停止暂停
    audioSource.UnPause();
}

//检测是否播放中
if (audioSource.isPlaying)
{
    print('正在播放');
}
else
{
    print('播放结束');
}
```。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91AudioSource-yin-pin-xiang-guan.html</guid><pubDate>Wed, 29 Apr 2026 09:30:28 +0000</pubDate></item><item><title>【unity入门】物理系统刚体加力</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-wu-li-xi-tong-gang-ti-jia-li.html</link><description># 物理系统刚体加力
`Rigidbody rigidBody;`
### 获取刚体组件
` rigidBody = GetComponent&lt;Rigidbody&gt;();`
### 增加力
**1.相对世界坐标系 第二个参数：力的模式**
```c#
rigidBody.AddForce(Vector3.forward * 10);
rigidBody.AddForce(this.transform.forward * 10); ;//相对自己坐标系
```

**2.相对本地坐标系 第二个参数：力的模式**
` rigidBody.AddRelativeForce(Vector3.forward * 10);`

### 扭矩的力
**1.相对世界坐标系 第二个参数：力的模式**
`rigidBody.AddTorque(Vector3.up * 10);`
**2.相对本地坐标系 第二个参数：力的模式**
`rigidBody.AddRelativeTorque(Vector3.up * 10);`

### 改变速度
相对世界坐标系
`rigidBody.velocity = Vector3.forward * 5;`

### 爆炸效果
(力的大小，爆炸中心，爆炸半径)
`rigidBody.AddExplosionForce(100, Vector3.zero, 10);`

## 力的模式
动量定理
Ft = mv
v = Ft / m

### Force 持续的力
与时间 质量有关
v = 10 * 0.02 / 2 = 0.1m / s
每物理帧移动：0.1m / s * 0.02 = 0.002m

###Acceleration 持续加速度
忽略质量(默认m = 1)
v = (0, 0, 10) * 0.02 / 1 = 0.2m / s
每物理帧移动：0.2m / s * 0.02 = 0.004m

### Impulse 瞬间的力
忽略时间(默认t = 1)
v = 10 * 1 / 2 = 5m / s
每物理帧移动：5m / s * 0.02 = 0.1m

### VelocityChange 瞬时速度
忽略质量 时间
v = 10 * 1 / 1 = 10m / s
每物理帧移动：10m / s * 0.02 = 0.2m

```c#
rigidBody.AddForce(Vector3.forward * 10, ForceMode.Acceleration);
rigidBody.AddForce(Vector3.forward * 10, ForceMode.Force);
rigidBody.AddForce(Vector3.forward * 10, ForceMode.Impulse);
rigidBody.AddForce(Vector3.forward * 10, ForceMode.VelocityChange);
```

unity自带组件Constant Force控制物体
控制力和扭矩力，位移和旋转
constant force 自动加刚体

## 刚体的休眠
```c#
if (rigidBody.IsSleeping())
{
 rigidBody.WakeUp();
}
```。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-wu-li-xi-tong-gang-ti-jia-li.html</guid><pubDate>Wed, 29 Apr 2026 09:24:06 +0000</pubDate></item><item><title>【unity入门】物理系统碰撞检测</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-wu-li-xi-tong-peng-zhuang-jian-ce.html</link><description># 物理系统碰撞检测
物理帧更新后检测物理碰撞触发相关函数 然后再执行逻辑帧更新
## 碰撞检测相关
### 碰撞触发接触（参数是碰自己的碰撞体）
```c#
private void OnCollisionEnter(Collision collision)
{
    //碰撞器
    collision.collider
    //对象
    collision.gameObject
    //位置
    collision.transform
    碰撞点数
    collision.contactCount
    碰撞点的坐标
    ContactPoint[] pos = collision.contacts;
    print(this.gameObject.name + '被' + collision.gameObject.name + '撞到了');
}
```
### 一直接触 有摩擦力
```c#
private void OnCollisionStay(Collision collision)
{
    print(this.gameObject.name + '和' + collision.gameObject.name + '一直接触');
}
```
### 退出接触
```c#
private void OnCollisionExit(Collision collision)
{
    print(this.gameObject.name + '被' + collision.gameObject.name + '结束碰撞');
}
```

## 触发器相关
### 开始触碰触发
```c#
private void OnTriggerEnter(Collider other)
{
    print(this.gameObject.name + '被' + other.gameObject.name + '接触到了');
}
```
### 重叠触发
```c#
private void OnTriggerStay(Collider other)
{
    print(this.gameObject.name + '一直在和' + other.gameObject.name + '重叠');
}
```
### 结束触碰触发
```c#
private void OnTriggerExit(Collider other)
{
    print(this.gameObject.name + '被' + other.gameObject.name + '结束接触了');
}
```
可以写虚函数来重写
重写的话写成projected就行了
unity自动通过反射帮我们调用。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-wu-li-xi-tong-peng-zhuang-jian-ce.html</guid><pubDate>Wed, 29 Apr 2026 09:12:21 +0000</pubDate></item><item><title>【unity入门】Camera摄像机相关</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91Camera-she-xiang-ji-xiang-guan.html</link><description># Camera摄像机相关
## 重要静态成员
如果有两个主摄像机就无法准确得到的是哪个
### 获取主摄像机
`print(Camera.main.name);`

### 取摄像机数量
`print(Camera.allCamerasCount);
`
### 得到所有摄像机
```c#
Camera[] arr = Camera.allCameras;
print(arr.Length);

```
### 渲染相关委托
摄像机剔除前处理的委托函数
`Camera.onPreCull += (c) =&gt; { };`
摄像机渲染前处理的委托
`Camera.onPreRender += (c) =&gt; { };`
摄像机渲染后处理的委托
`Camera.onPostRender += (c) =&gt; { };`

## 重要成员
获取界面上的参数
### 设置深度
`Camera.main.depth = 10;`

### 世界坐标转屏幕坐标
z轴是物体与摄像机的距离
做血条
`Vector3 v = Camera.main.WorldToScreenPoint(this.transform.position);`。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91Camera-she-xiang-ji-xiang-guan.html</guid><pubDate>Wed, 29 Apr 2026 09:06:55 +0000</pubDate></item><item><title>【unity入门】Screen屏幕相关</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91Screen-ping-mu-xiang-guan.html</link><description># Screen屏幕相关
## 当前屏幕分辨率 （整个电脑的）
```c#
Resolution r = Screen.currentResolution;
print('当前屏幕分辨率宽：' + r.width + '高：' + r.height);
```

## 游戏窗口的分辨率
```c#
print(Screen.width);
print(Screen.height);
```

##屏幕休眠模式
`Screen.sleepTimeout = SleepTimeout.NeverSleep;`

## 静态方法
设置分辨率 移动设备不用
`Screen.SetResolution(1920, 1080, false);//是否全屏`。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91Screen-ping-mu-xiang-guan.html</guid><pubDate>Wed, 29 Apr 2026 08:58:39 +0000</pubDate></item><item><title>【unity入门】Input输入相关</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91Input-shu-ru-xiang-guan.html</link><description># Input输入相关
## 鼠标键盘检测输入 
```c#
if (Input.GetMouseButton(0))
 {
     print('左键长按');
 }
 print(Input.mouseScrollDelta * 10);

 if (Input.GetKeyDown(KeyCode.Space))
 {
     print('空格按下');
 }
 if (Input.GetKey('escape'))
 {
     print('esc按下');
 }

```
## 通过wasd获取-1~1的值
```c#
 print(Input.GetAxis('Horizontal'));
 print(Input.GetAxis('Vertical'));
```
## 通过鼠标移动获取-1~1的值
```c#
 print(Input.GetAxis('Mouse X'));
 print(Input.GetAxis('Mouse Y'));
```

 GetAxisRaw无小数。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91Input-shu-ru-xiang-guan.html</guid><pubDate>Wed, 29 Apr 2026 08:54:42 +0000</pubDate></item><item><title>【unity入门】坐标系转换相关（世界与本地）</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-zuo-biao-xi-zhuan-huan-xiang-guan-%EF%BC%88-shi-jie-yu-ben-di-%EF%BC%89.html</link><description># 坐标系转换相关（世界与本地）
## 世界坐标系转本地坐标系
### 点的转换
受缩放影响
`print('转换后的点' + this.transform.InverseTransformPoint(Vector3.forward));`

### 方向向量的转换
不受缩放影响
`print('转换后的方向' + this.transform.InverseTransformDirection(Vector3.forward));`
受缩放影响
`print('转换后的方向' + this.transform.InverseTransformVector(Vector3.forward));`

## 本地转世界
### 点的转换
受缩放影响
`print('本地转世界的点' + this.transform.TransformPoint(Vector3.forward));
`
### 方向
不受缩放影响
`print('本地转世界的方向' + this.transform.TransformDirection(Vector3.forward));`
受缩放影响
`print('本地转世界的方向' + this.transform.TransformVector(Vector3.forward));`。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-zuo-biao-xi-zhuan-huan-xiang-guan-%EF%BC%88-shi-jie-yu-ben-di-%EF%BC%89.html</guid><pubDate>Wed, 29 Apr 2026 08:50:54 +0000</pubDate></item><item><title>【unity入门】Transform父子关系</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91Transform-fu-zi-guan-xi.html</link><description># Transform父子关系
## 获取父对象
获取父对象
`print(this.transform.parent.name);`
移除父对象
`this.transform.parent = null;`
更换父对象 不用置空也可以更换父对象
`this.transform.parent = GameObject.Find('Father2').transform;`

通过API
移除父对象
`this.transform.SetParent(null);`
设置父对象
`this.transform.SetParent(GameObject.Find('Father2').transform);`

**参数一：父对象
参数二：是否保留世界坐标**
true: 和原本位置一样 并且还是会和父对象进行参考计算, 缩放位置会随父对象变化
false: 不再和父对象进行计算了，世界坐标是啥样的进来也是啥样的，参考对象变成父对象，该物体可能会放大缩小或者位置发生改变
`this.transform.SetParent(GameObject.Find('Father3').transform, false);`


## 抛妻弃子
断绝父子关系 没办法断绝子孙关系
`this.transform.DetachChildren();`

## 获取子对象
找子对象
按名字查找
失活的也可以找到 GameObject的不能找到失活的
`print(this.transform.Find('Cube (1)').name);`
只能找儿子不能找孙子
`print(this.transform.Find('GameObject').name);`

遍历儿子
获得儿子的数量(包括失活的儿子，不包括孙子)
`print(this.transform.childCount);`
通过索引获得儿子 返回值transform可以得到位置信息
this.transform.GetChild(0);//溢出报错
```
for (int i = 0; i &lt; this.transform.childCount; i++)
{
    print(this.transform.GetChild(i).name);
}
```

## 儿子的操作
判断父对象是谁
判断Son是不是我的儿子
失活的也可以
### 一个对象判断自己是不是另一个对象的儿子
```
if (son.IsChildOf(this.transform))
{
    print(son + '是我的儿子');
}
```

### 得到自己作为的编号
`print(son.GetSiblingIndex());`
### 把自己设置成第一个儿子
`son.SetAsFirstSibling();`
###把自己设置成最后一个儿子
`son.SetAsLastSibling();`
### 把自己设置成指定儿子
```
son.SetSiblingIndex(15);//就算填的溢出也不报错 会变成最后一个编号
son.SetSiblingIndex(-1);//超出范围都是最后一个编号
```。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91Transform-fu-zi-guan-xi.html</guid><pubDate>Wed, 29 Apr 2026 08:40:44 +0000</pubDate></item><item><title>【unity入门】缩放和看向</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-suo-fang-he-kan-xiang.html</link><description># 缩放和看向
## 缩放
### 相对世界坐标系
` print(this.transform.lossyScale);`
### 相对本地（父对象）
` print(this.transform.localScale);`

 缩放世界坐标系不能改 只能得
 只能改相对父对象的
` this.transform.localScale = new Vector3(3, 3, 3);`

缩放没有API只能自己写
`this.transform.localScale += Vector3.one * Time.deltaTime;`
 
## 看向
一直看向原点 相对于世界坐标系
` this.transform.LookAt(Vector3.zero);`
看向一个对象
` this.transform.LookAt(lookAtObj);`
。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-suo-fang-he-kan-xiang.html</guid><pubDate>Wed, 29 Apr 2026 08:32:15 +0000</pubDate></item><item><title>【unity入门】角度和旋转</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-jiao-du-he-xuan-zhuan.html</link><description> # 角度相关
相对世界坐标角度
`print(this.transform.eulerAngles);`
相对父对象角度
`print(this.transform.localEulerAngles);`
不能单独改变一个坐标 三个一起
`this.transform.localEulerAngles = new Vector3(10, 10, 10);`

# 旋转
第一个参数 旋转的角度
第二个参数 默认不填围绕自己旋转
```
this.transform.Rotate(new Vector3(0, 10, 0) * Time.deltaTime);
this.transform.Rotate(new Vector3(0, 10, 0) * Time.deltaTime, Space.World);
```

相对于轴转多少度
参数一：相对于哪个轴转动
参数二：角度
参数三：默认自己坐标系
`this.transform.Rotate(Vector3.right, 10 * Time.deltaTime);`

相对于某一个点转
参数一：相对于哪一个点转圈圈
参数二：相对于那一个点的 哪一个轴转圈圈
参数三：转的度数 旋转速度*时间
`this.transform.RotateAround(Vector3.zero, Vector3.right, 10 * Time.deltaTime);`
。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-jiao-du-he-xuan-zhuan.html</guid><pubDate>Wed, 29 Apr 2026 08:27:23 +0000</pubDate></item><item><title>【unity入门】位移相关</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-wei-yi-xiang-guan.html</link><description># 位移相关
## Vector3基础
三维坐标的一个点，向量
### 申明一个点
```c#
Vector3 v = new Vector3();
v.x = 10;
v.y = 10;
v.z = 10;
```
只传xy，默认z为零
`Vector3 v2 = new Vector3(10, 10);`
一步到位
`Vector3 v3 = new Vector3(10, 10, 10);`

```c#
Vector3 v4;
v4.x = 10;
v4.y = 10;
v4.z = 10;
```

### 加减乘除
```c#
Vector3 v1 = new Vector3(1, 1, 1);
Vector3 v12 = new Vector3(2, 2, 2);
print(v1 + v12);
print(v1 - v12);

print(v1 * 10);
print(v12 / 2);

print(Vector3.zero);//000

print(Vector3.right);//100
print(Vector3.left);//-100

print(Vector3.forward);//001
print(Vector3.back);//00-1

print(Vector3.up);//010
print(Vector3.down);//0-10
```

### 计算两点之间的距离
`print(Vector3.Distance(v1,v12));
`

## 位置
### 相对世界坐标系
`print(this.transform.position);`
### 相对父对象
`print(this.transform.localPosition);`

位置不能单独修改一个，只能整体三个数值都一起改变
```c#
this.transform.position = new Vector3(10, 10, 10);
this.transform.localPosition = Vector3.up * 10;
```

如果想只改变一个值
`this.transform.position = new Vector3(10, this.transform.position.y, this.transform.position.z);`
取出来再改
```c#
Vector3 vv = this.transform.localPosition;
vv.x = 100;
```

### 获取当前对象的方向
```c#
print(this.transform.forward);
print(this.transform.up);
print(this.transform.right);
```

## 位移
`this.transform.position += this.transform.forward * 1 * Time.deltaTime;
`
API
第一个参数：路程 = 方向*速度*时间
第二个参数：相对于世界/自己的坐标系

世界的世界 = 世界
`this.transform.Translate(Vector3.forward * 1 * Time.deltaTime, Space.World);`

世界的自己 = 自己
`this.transform.Translate(this.transform.forward * 1 * Time.deltaTime, Space.World);`

自己世界坐标下的自己坐标系 （十分的奇怪，不会这样让物体动）
`this.transform.Translate(this.transform.forward * 1 * Time.deltaTime, Space.Self);`

自己的世界 = 自己面朝向
`this.transform.Translate(Vector3.forward * 1 * Time.deltaTime, Space.Self);`。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-wei-yi-xiang-guan.html</guid><pubDate>Wed, 29 Apr 2026 08:14:07 +0000</pubDate></item><item><title>【unity入门】时间相关Time</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-shi-jian-xiang-guan-Time.html</link><description># 时间相关Time
## 时间缩放比例
时间暂停
`Time.timeScale = 0;`
恢复正常
`Time.timeScale = 1;`
两倍速
`Time.timeScale = 2;`

# 帧间隔时间
一般用来位移
路程 = 时间 * 速度

最近一帧，用了多少时间
### 受Scale影响
`print('受Scale影响'+Time.deltaTime);`
### 不受Scale影响
`print('不受Scale影响' + Time.unscaledDeltaTime);`

**为什么移动时都要乘Time.deltaTime？**
高帧设备帧数多，每一帧时间短距离小（一帧移动的距离：10 × 0.016 = 0.16m）
低帧设备帧数少，每一帧时间长距离大（一帧移动的距离：10 × 0.033 = 0.33m）
这样相同时间移动的距离就是一样的
（当前帧移动距离=每秒速度*这一帧时间）

# 游戏开始到现在的时间
一般用在单机游戏 计时

### 受Scale影响
`print('受Scale影响' + Time.time);`
不受Scale影响
`print('受Scale影响' + Time.unscaledTime);`

### 从开始到现在游戏跑了多少帧
`print(Time.frameCount);`

# 物理帧间隔时间
### 受Scale影响
print(Time.fixedDeltaTime);
### 不受Scale影响
print(Time.fixedUnscaledDeltaTime);。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-shi-jian-xiang-guan-Time.html</guid><pubDate>Wed, 29 Apr 2026 07:57:32 +0000</pubDate></item><item><title>【unity入门】最小单位GameObject</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-zui-xiao-dan-wei-GameObject.html</link><description># 最小单位GameObject
## GameObject的成员变量
### 名字
`print(this.gameObject.name);`
### 改名
this.gameObject.name = 'Lesson4新改的名字';
`print(this.gameObject.name);`
### 是否激活
`print(this.gameObject.activeSelf);`
### 是否静态
`print(this.gameObject.isStatic);`
### 层级layer
`print(this.gameObject.layer);`
### 标签tag
`print(this.gameObject.tag);`

```c#
print(this.transform.position);
print(this.gameObject.transform.rotation);
```
this.gameObject.transform == this.transform

# GameObject的静态方法
## 创建几何体
```c#
GameObject obj = GameObject.CreatePrimitive(PrimitiveType.Cube);
obj.name = '小林的正方体';
```
可以通过obj.GetComponent获取正方体的脚本信息

## 查找场景中的一个对象(效率低，会遍历所有场景)
都没办法找到失活的对象
如果有同名的无法找到指定的对象，只能随机找
### 根据名字查找
```c#
GameObject obj2 = GameObject.Find('小林的正方体');
if (obj2 != null)
{
    print('找到了' + obj2.name);
}
else
{
    print('没找到');
}
```
### 通过Tag来查找对象
如果有两个对象都是Player标签，无法找到指定的对象，只能随机找
```c#
GameObject obj3 = GameObject.FindWithTag('Player');
//GameObject.FindGameObjectsWithTag('Player'); //跟上面的一模一样
if (obj3 != null)
{
    print('找到了palyer标签的' + obj3.name);
}
else
{
    print('没找到');
}
```
### 查找多个对象 只能通过Tag
只能找到激活对象
```c#
GameObject[] objs = GameObject.FindGameObjectsWithTag('Player');
print('找到tag为Palyer的对象个数' + objs.Length);
```
Unity中的Object是Unity自定义类,也是继承万物之父object，命名空间UnityEngine
万物之父object，命名空间system

### 找到场景中的脚本
先遍历了对象再从各个对象中找有没有这个脚本，效率低
```c#
Lesson4 o = GameObject.FindObjectOfType&lt;Lesson4&gt;();
print(o.gameObject.name);
```

## 克隆对象
对象继承MonoBehaviour，不用写GameObject，这个方法在Object里
（克隆对象 位置 旋转量）
`GameObject obj5 = GameObject.Instantiate(myObject);`
之后再来操作这个克隆对象

## 删除对象 
继承MonoBehaviour，不用写GameObject，这个方法在Object里
先打一个删除标签，下一帧才删除，避免卡顿
`GameObject.Destroy(deleteObj);`
延迟五秒删除
`GameObject.Destroy(obj5, 5);`
删除自己这个脚本
`GameObject.Destroy(this);`
马上在这一帧就删除了
`GameObject.DestroyImmediate();`


### 过场景不被删除
默认切换场景删除场景中的对象
一般情况下是自己这个对象
继承MonoBehaviour，不用写GameObject，这个方法在Object里
`GameObject.DontDestroyOnLoad(this.gameObject);`

# GameObject的成员方法
## 创建空物体
```c#
GameObject obj6 = new GameObject();
GameObject obj7 = new GameObject('小林创建的空物体的名字');
GameObject obj8 = new GameObject('小林创建的空物体，顺便加了俩脚本', typeof(Lesson2), typeof(Lesson3));
```

## 为对象添加脚本
```c#
Lesson2 les1 = obj6.AddComponent(typeof(Lesson2)) as Lesson2;
Lesson2 les2 = obj6.AddComponent&lt;Lesson2&gt;();
```
//之后再进行一些处理

## 得到脚本的方法，和之前继承mono的一样

## 标签比较
```c#
if (this.gameObject.CompareTag('Player'))
{
    print('对象的标签是 Player');
}
if (this.gameObject.tag =='Player')
{
    print('对象的标签是 Player');
}
```

## 让对象失活
```c#
obj6.SetActive(false);
obj7.SetActive(false);
obj8.SetActive(false);
```

## 执行自己身上所有脚本的该方法
### 效率低每个脚本都找 广播行为
`this.gameObject.SendMessage('TestFun');`
### 让自己和自己的子对象执行
`this.gameObject.BroadcastMessage('函数名');`
### 让自己和自己的父对象执行
`this.gameObject.SendMessageUpwards('函数名');`。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-zui-xiao-dan-wei-GameObject.html</guid><pubDate>Wed, 29 Apr 2026 07:08:17 +0000</pubDate></item><item><title>【unity入门】MonoBehaviour中的重要内容</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91MonoBehaviour-zhong-de-zhong-yao-nei-rong.html</link><description># MonoBehaviour中的重要内容
## 获取依附的GameObject信息
### 名字
       print(this.gameObject.name);
### 位置信息
       **位置** print(this.transform.position);
       **角度** print(this.transform.eulerAngles);
       **缩放** print(this.transform.lossyScale);
（this.gameObject.transform和this.transform一样）
### 获取脚本是否激活
     this.enabled = false;
### 得到其他脚本对象的信息
     print(OtherLesson.gameObject.name);
     print(OtherLesson.transform.position);
     ......

## 关于获取脚本
只要能得到场景中其他对象或者其他对象的脚本，就能得到它的所有信息
## 获得自己同个GameObject的不同脚本 
只能得单个，不能同个脚本有多个
获取失败返回空
### 历史替换
     Lesson3_Test t = this.GetComponent('Lesson3_Test') as Lesson3_Test;
     if (t != null)    print(t);
### 通过Type获得
     t = this.GetComponent(typeof(Lesson3_Test)) as Lesson3_Test;
     if (t != null)    print(t);
### 根据泛型获取
     t = this.GetComponent&lt;Lesson3_Test&gt;();
     if (t != null)    print(t);  

### 获得自己多个脚本
一般不会再同个对象中挂几个相同脚本
```c#
     Lesson3[] array = this.GetComponents&lt;Lesson3&gt;();
     print(array.Length);
     List&lt;Lesson3&gt; list = new List&lt;Lesson3&gt;();
     this.GetComponents&lt;Lesson3&gt;(list);
     print(list.Count);
```
  
### 得到子对象的脚本（默认也会找自己身上有没有该脚本）
括号参数默认false 检测gameobject子对象不失活的 true失活也会找
**1.找单个子对象**
```c#
     t = this.GetComponentInChildren&lt;Lesson3_Test&gt;(false);
     print(t);
```
 **2.多个子对象**
```c#
     Lesson3_Test[] arr = this.GetComponentsInChildren&lt;Lesson3_Test&gt;();
     print(arr.Length);
     List&lt;Lesson3_Test&gt; lis = new List&lt;Lesson3_Test&gt;();
     GetComponentsInChildren&lt;Lesson3_Test&gt;(true, lis);
     print(lis.Count);
```
### 得到父对象的脚本(默认找自己的)
括号没有参数，如果父对象失活子对象也失活，就没办法执行脚本
**1.单个父对象**
```c#
     t = this.GetComponentInParent&lt;Lesson3_Test&gt;();
     print(t);
```
**2.多个父对象(所有父类都找)**
```c#
     arr = this.GetComponentsInParent&lt;Lesson3_Test&gt;();
     print(arr.Length);
```
### 尝试获取单个脚本 找到true
正常没找到报空 更安全
```c#
     Lesson3_Test test;
     if(this.TryGetComponent&lt;Lesson3_Test&gt;(out test) {  }
```。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91MonoBehaviour-zhong-de-zhong-yao-nei-rong.html</guid><pubDate>Wed, 29 Apr 2026 06:59:13 +0000</pubDate></item><item><title>【unity入门】脚本的基本规则</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-jiao-ben-de-ji-ben-gui-ze.html</link><description>1.脚本必须继承MonoBehaviour才能挂载在GameObje上
2.继承了MonoBehaviour的脚本不能new，只能挂载
3.继承了MonoBehaviour不要写构造函数
4.一个对象可以挂在同一个继承了MonoBehaviour的脚本
5.继承了MonoBehaviour的类可以再次被继承。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-jiao-ben-de-ji-ben-gui-ze.html</guid><pubDate>Wed, 29 Apr 2026 06:16:36 +0000</pubDate></item><item><title>【unity入门】生命周期函数</title><link>https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-sheng-ming-zhou-qi-han-shu.html</link><description># 【unity入门】生命周期函数

## 基本概念
unity会按顺序执行以下生命周期函数
`Awake()`
`OnEnable()`
`Start()`
`FixedUpdate()`
`Update()`
`LateUpdate()`
`OnDisable()`
`OnDestroy()`


### Awake和Start的区别
1.当物体失活，脚本激活时Awake仍然执行，只要对象创建并且脚本激活就会执行
2.Start必须在物体处于激活状态才会执行

### 物理帧更新`FixedUpdate()`
可以在ProjectSetting中的Time设置，代表间隔几秒执行一次

### 其他
只要脚本失活所有函数都不执行

```c#
private void Awake()
{
    print('Awake 该类的对象被创建时');       
}
private void OnEnable()
{
    print('OnEnable 激活时调用');
}
private void Start()
{
    print('Start');
}
private void FixedUpdate()
{
    print('FixedUpdate');
}
private void Update()
{
    print('Update');
}
private void LateUpdate()
{
    print('LateUpdate');
}
private void OnDisable()
{
    print('OnDisable失活');
}
private void OnDestroy()
{
    print('OnDestroy删除');
}
```。</description><guid isPermaLink="true">https://linarisx.github.io/lynn.github.io/post/%E3%80%90unity-ru-men-%E3%80%91-sheng-ming-zhou-qi-han-shu.html</guid><pubDate>Wed, 29 Apr 2026 06:12:49 +0000</pubDate></item></channel></rss>